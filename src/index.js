import React from "react";
import ReactDOM from "react-dom";

const jsxElement = <h1>This is a JSX element in headig</h1>;
const rootElement = document.getElementById("root");

ReactDOM.render(jsxElement, rootElement);
